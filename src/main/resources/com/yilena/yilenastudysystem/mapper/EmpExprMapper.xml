<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yilena.yilenastudysystem.mapper.EmpExprMapper">

    <insert id="saveEmpExpr">
        insert into emp_expr (emp_id, begin, end, company, job) values
        <foreach collection="exprList" item="expr" separator=",">
            (#{expr.empId}, #{expr.begin}, #{expr.end}, #{expr.company}, #{expr.job})
        </foreach>
    </insert>

    <delete id="deleteEmpExprByEmpId">
        delete from emp_expr where emp_id in
        <foreach item="id" collection="ids" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
    <select id="findById" resultType="com.yilena.yilenastudysystem.pojo.EmpExpr">
        select id, emp_id as empId, begin, end, company, job from emp_expr where emp_id = #{empId}
    </select>
</mapper>
